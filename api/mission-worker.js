const { runtime, json, error } = require("./_mission-runtime");
module.exports = async function handler(req, res) { try { if (req.method !== "POST") return json(res, 405, { ok: false, error: "method_not_allowed" }); const supplied = String(req.headers.authorization || "").replace(/^Bearer\s+/i, ""); const expected = process.env.MISSION_WORKER_SECRET_V2 || process.env.MISSION_WORKER_SECRET; if (!expected || supplied !== expected) return json(res, 401, { ok: false, error: "unauthorized" }); const { runner } = runtime(); await runner.recoverStale(); const active = await runner.tick(); await Promise.allSettled([...runner.inFlight]); return json(res, 200, { ok: true, processed: active }); } catch (raw) { return error(res, raw); } };
